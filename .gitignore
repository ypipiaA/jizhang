# Python 本地预览工具产生的文件（不参与部署）
__pycache__/
*.pyc
.venv/
.idea/

# 本地数据库（旧版数据，属于个人数据不上传）
records.db

# 部署临时目录（Pages 直连仓库后不再需要）
deploy/

# Claude 本地配置
.claude/

# 构建产物与缓存
dist/
.chart.cache.js
node_modules/

# 安卓构建缓存和本机签名密钥（升级安装需要保留，勿提交）
.android-build/
.android-signing/
android/app/src/main/assets/
public/downloads/*.apk

# 本机环境凭证
.env
.env.*
!.env.example
