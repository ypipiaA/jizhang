# 项目与发布边界

本目录是「生活记账」，唯一正式 Pages 项目为 `jizhang`，网址为 `https://jizhang-d9k.pages.dev/`。

- Pages 发布使用本目录 `cf_deploy.py`，保留目标与应用身份校验。不得用临时命令、另一项目的部署脚本或环境变量绕过校验。
- `jiaban / jiaban-x2m.pages.dev` 属于 `F:/py/jiaban` 的「加班记录」。生活记账的任何资源、Worker 或安装包都不得发布到该站点，也不得修改其数据库绑定。
- 保留生活记账现有的应用身份、签名、数据库和用户数据。项目之间不要复制或替换构建产物。
- Pages 项目与独立 Worker 是不同的发布目标；不要把 `wrangler deploy` 的成功当作 Pages 已更新。每次发布核对实际域名和首页。
- 不在输出、公开资源或分享文件中暴露访问令牌、签名材料或用户同步口令。

## 发布错误记录（必须遵守）

2026-09-09 用户指出：前一天曾把生活记账网页发布到其他网站。这是发布目标混淆错误，后续任务必须据此执行以下检查：

- 发布前确认当前目录、待发布应用身份、远端 Pages 项目名和正式域名四者一致。不得凭相似名称、最近访问的项目或其他项目的成功日志选择目标。
- 默认发布命令、说明文档和实际操作都必须经过本目录 `cf_deploy.py` 的校验；校验失败时停止，不得改用网页手动上传、临时命令或修改目标变量来绕过。
- 发布后实际访问 `https://jizhang-d9k.pages.dev/`，确认首页是生活记账；涉及 APK 时，还要核对实际下载文件与本项目安装包一致。验证完成后才能向用户报告发布成功。
- 修复本项目问题时也不得操作其他项目的资源、部署、签名或数据库绑定。
